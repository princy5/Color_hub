import React from 'react'
import { Header, Sidebar, Window } from ".";

const Layout = () => {
    return (
        <React.Fragment>
            <Header />
            <Sidebar />
        </React.Fragment>
    )
}

export default Layout
